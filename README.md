# PYTHON-MYSQL-encryption
A Python-based MySQL database connectivity project that securely handles database passwords using Fernet symmetric encryption. The project generates and stores an encryption key, encrypts the MySQL password, decrypts it when required, and uses the decrypted password to establish a connection with MySQL
